import { ArticleTypeColorPipe } from './article-type-color.pipe';

describe('ArticleTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
